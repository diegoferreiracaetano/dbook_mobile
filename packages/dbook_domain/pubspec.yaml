name: dbook_domain
description: Entidades, portas e casos de uso do DBook — Dart puro, sem Flutter.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.13.3

resolution: workspace

dependencies:
  freezed_annotation: ^3.1.0

dev_dependencies:
  build_runner: ^2.16.1
  coverage: ^1.9.2
  freezed: ^4.0.1
  lints: ^6.0.0
  test: ^1.25.6

name: dbook_feature_flights
description: Feature de busca e listagem de voos do DBook — telas, estado e navegação.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.13.3
  flutter: ">=1.17.0"

resolution: workspace

dependencies:
  dbook_core_network:
    path: ../dbook_core_network
  dbook_core_session:
    path: ../dbook_core_session
  dbook_design_system:
    path: ../dbook_design_system
  dbook_domain:
    path: ../dbook_domain
  flutter:
    sdk: flutter
  flutter_riverpod: ^3.4.3
  freezed_annotation: ^3.1.0
  go_router: ^16.2.4
  intl: ^0.20.2
  shared_preferences: ^2.5.3

dev_dependencies:
  build_runner: ^2.16.1
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
  freezed: ^4.0.1

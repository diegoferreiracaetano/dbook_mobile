/// Cliente Dio autenticado (token + refresh automático), compartilhado por
/// toda feature do DBook que precisa chamar a API.
library;

export 'src/dbook_auth_interceptor.dart';
export 'src/session_providers.dart';

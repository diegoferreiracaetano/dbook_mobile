name: dbook_core_session
description: Cliente Dio autenticado (token + refresh automático), compartilhado por toda feature do DBook.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.13.3
  flutter: ">=1.17.0"

resolution: workspace

dependencies:
  dbook_core_network:
    path: ../dbook_core_network
  dbook_core_storage:
    path: ../dbook_core_storage
  dbook_domain:
    path: ../dbook_domain
  dio: ^5.9.0
  flutter:
    sdk: flutter
  flutter_riverpod: ^3.4.3

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

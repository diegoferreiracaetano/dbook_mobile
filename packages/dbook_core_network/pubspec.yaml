name: dbook_core_network
description: Cliente HTTP, DTOs e mapeamento pra API do DBook — Dart puro.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.13.3

resolution: workspace

dependencies:
  dbook_domain:
    path: ../dbook_domain
  dio: ^5.9.0
  freezed_annotation: ^3.1.0
  json_annotation: ^4.12.0

dev_dependencies:
  build_runner: ^2.16.1
  freezed: ^4.0.1
  json_serializable: ^6.11.1
  lints: ^6.0.0
  test: ^1.25.6

name: dbook_feature_ai
description: Sugestão de voo por IA do DBook — busca em linguagem natural.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.13.3
  flutter: ">=1.17.0"

resolution: workspace

dependencies:
  dbook_core_network:
    path: ../dbook_core_network
  dbook_core_session:
    path: ../dbook_core_session
  dbook_design_system:
    path: ../dbook_design_system
  dbook_domain:
    path: ../dbook_domain
  flutter:
    sdk: flutter
  flutter_riverpod: ^3.4.3
  freezed_annotation: ^3.1.0

dev_dependencies:
  build_runner: ^2.16.1
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
  freezed: ^4.0.1
